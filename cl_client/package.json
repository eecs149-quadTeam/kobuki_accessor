{
  "name": "cl_client",
  "version": "1.0.0",
  "description": "Command line client for a web server to mock the controller sending data to the Kobuki accessors.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dennis Zhao",
  "license": "ISC",
  "dependencies": {
    "ws": "^0.8.0"
  }
}
